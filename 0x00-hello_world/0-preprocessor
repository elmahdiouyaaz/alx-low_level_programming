#!/bin/bash

# Check if $CFILE is set
if [ -z "$CFILE" ]; then
  echo "Error: \$CFILE is not set."
  exit 1
fi

# Run the C preprocessor and save the result to the file 'c'
gcc -E "$CFILE" -o c

echo "Preprocessing completed. Output saved in 'c'.
