#include <stdio.h>
/** 
 * The main function is the entry point of the program.
 * It prints a string to the standard output using the 
 * puts function and returns 0 to indicate successful 
 * termination of the program.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
